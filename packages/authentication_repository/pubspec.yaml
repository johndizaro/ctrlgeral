name: authentication_repository
description: Dart package which manages the authentication domain.
publish_to: none
version: 1.0.0+1

environment:
  sdk: ">=2.12.0-0 <3.0.0"
dependencies:
  flutter:
    sdk: flutter

  firebase_auth: ^1.4.1
